#ifndef MACROLIKE_H
#define MACROLIKE_H
/**
 * 3-function_like_macro.h - file name
 *
 * @x: input value
 * Desc: header that computes an absolute value
 */
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
